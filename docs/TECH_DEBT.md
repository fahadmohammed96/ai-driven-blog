# TECH_DEBT — registro del debito tecnico

Regola (vedi DEVELOPMENT.md §6): **niente debito silenzioso.** Ogni scorciatoia consapevole va qui, con un **trigger di rientro**. Nel codice, un `// TODO(debt): <ID>` linka la voce. A fine fase, il **debt-gate** paga ciò che è scaduto.

Stato: `OPEN` · `PAYING` · `PAID`.

| ID | Debito | Perché l'abbiamo preso | Rischio/costo | Trigger di rientro | Stato |
|----|--------|------------------------|---------------|--------------------|-------|
| DEBT-001 | Repo dentro **OneDrive** (`...\OneDrive\Desktop\...`) | Procedere subito col setup senza spostare la cartella | Sync/lock dei file, performance scarsa, conflitti con Docker/WSL2 | **Prima di scrivere codice vero** (inizio Fase 0, task scaffold) | **PAID** |
| DEBT-002 | Migrazioni DB **a mano** (SQL in `apps/api/drizzle/*.sql`); drizzle-kit non ancora cablato | Evitare pairing di versioni drizzle-kit/orm e gestione journal mentre lo schema è minimo | Possibile drift tra `schema.ts` e SQL (mitigato: le query Drizzle nei test falliscono se divergono) | **Prima di Fase 1 (content)** o quando lo schema cresce | **PAID** |
| DEBT-003 | **Branch protection** non attiva su `main` (la CI non blocca davvero il merge) | Richiede admin GitHub/API non disponibili da qui (gh non installato, nessun token) | Una PR rossa resta tecnicamente mergeabile finché non si abilita la protezione del branch | **Prima del primo collaboratore** — *in attesa: l'utente la abilita a mano (GitHub → Settings → Branches → require check `CI`)* | **PAID** |
| DEBT-004 | Layer HTTP Nest (controller/guard) non coperto da test **automatici** in CI; coperto da unit test sulla logica (`AuthService`) + e2e manuale | Testare DI/HTTP di Nest in Vitest richiede un transform (es. swc) per i decorator metadata — rimandato | Regressioni nel wiring HTTP non intercettate dalla CI | **Prima di aggiungere altri endpoint** / prima del multi-utente | **PAID** |
| DEBT-005 | **A runtime l'app si connette come superuser** Postgres (`blogs`) → la RLS è *bypassata a runtime* (è enforce solo nei test, via ruolo `NOSUPERUSER`) | n=1 dogfooding: la compose espone solo il superuser; creare un ruolo app dedicato + grant è lavoro di hardening non necessario finché il tenant è uno | Se arrivasse un tenant #2 senza hardening, niente isolamento reale a runtime (i dati sono comunque marcati `tenant_id` e l'isolamento è provato nei test) | **Prima del tenant #2** — ruolo app `NOSUPERUSER` + `GRANT` mirati + connection string dedicata (coerente con ADR-0002) | **PAID** |
| DEBT-006 | **Contract test del canale = validazione parziale** dall'OpenAPI (campi richiesti + security Bearer derivati dallo spec), non l'intero schema via runtime (Prism/openapi-backend) | Spin-up di un runtime OpenAPI completo è infrastruttura extra non necessaria col primo connettore; i campi/obblighi chiave bastano a verificare l'aderenza del connector | Un drift di tipo/forma non sui campi `required` potrebbe non essere intercettato dal contract test | **Al secondo connettore** o prima del go-live integrazioni → adottare un validatore OpenAPI runtime | **OPEN** |
| DEBT-007 | **Email = solo adapter SMTP** (Mailhog dev / relay): l'adapter provider API (SES/Postmark) di ADR-0018 non è implementato | Il test di accettazione è verso Mailhog (SMTP) e n=1 non invia ancora a volumi; il provider-port è già la cucitura giusta | In prod a volumi, SMTP "fai-da-te" ha deliverability/reputation peggiori di un provider | **Prima del primo invio di produzione a volumi** → implementare `EmailPort` su provider API | **OPEN** |
| DEBT-008 | **Nessun flusso di onboarding OAuth** del canale + gestione `CONNECTOR_SECRET_KEY`: il connector presume credenziali già nello store cifrato | Il contract test prova OAuth/refresh/rate-limit/cifratura senza bisogno della UI di "connetti canale"; nessun canale reale collegato in dogfooding | Collegare un canale reale richiede il consent-flow OAuth + provisioning sicuro della chiave master | **Prima di collegare un canale reale** → connect-flow OAuth + key management | **OPEN** |
| DEBT-009 | **Nessun endpoint di lista dei ContentItem**: `modules/content` espone solo `GET /articles/:id` (lettura singola), non una collezione filtrabile | La UI hub di slice 0 sono solo placeholder: non serve ancora un read-model di lista; il backend Fase 0–2.5 non ne aveva bisogno | La Library (slice 1) non ha sorgente dati: non può elencare/filtrare i contenuti né mostrare i badge di stato | **All'inizio di slice 1 (Library)** → endpoint `GET /articles` (o read-model di lista) in `modules/content`, con filtro per type/stato | **PAID** |
| DEBT-010 | **La generazione articolo non legge la brand voice dalle Settings**: il generatore travel usa la costante hard-coded `FOUNDER_VOICE` (`verticals/travel/itineraries.controller.ts`), mentre le Settings (slice 4) persistono `brandVoice {tone, audience}` per-tenant | Slice 4 = la **superficie** Settings; cablare il generatore era fuori scope e n=1 ha una sola voce (la costante coincide col founder) | Una modifica della brand voice nelle Settings **non** cambia la voce con cui l'AI genera → la setting appare ignorata; per il tenant #2 sarebbe sbagliata | **Quando la generazione deve onorare la voce configurata** (o al tenant #2) → far leggere `brandVoice` da `getTenantSettings` invece della costante | **OPEN** |
| DEBT-011 | **Pagamento acconto = solo stub deterministico** (`PaymentPort`/`StubPaymentClient` in `modules/commerce`): l'adapter Stripe **test-mode** dietro `STRIPE_SECRET_KEY` di [ADR-0023](adr/0023-commerce-trips-departures-payment-port.md) non è implementato | Il test di accettazione (journey *book→acconto→conferma*) è verso uno stub al confine — niente rete/chiavi/non-determinismo in CI, come l'LLM (Fase 1)/Mailhog (Fase 2)/connector-stub (Fase 2.5); il `PaymentPort` è già la cucitura giusta | Nessun incasso reale: PaymentIntent, `client_secret`, conferma via **webhook** (`payment_intent.succeeded`), 3DS/SCA, ricevute, rimborsi/cancellazioni non esistono | **Prima del primo incasso reale di un acconto** → `StripePaymentClient implements PaymentPort` (test-mode) + endpoint webhook con verifica firma + idempotency-key + key management (coerente con DEBT-008); segue follow-up del fondatore | **OPEN** |
| DEBT-013 | **Sorgenti analytics esterne (GA4 + Search Console) = solo stub deterministico** (`Ga4SourceStub`/`SearchConsoleSourceStub` in `modules/analytics`): gli adapter reali su GA4 Data API / Search Console API di [ADR-0025](adr/0025-unified-analytics-source-port.md) non sono implementati | L'accettazione (*una dashboard mostra metriche cross-canale*) è verso stub al confine — niente API/chiavi/rete/non-determinismo in CI, come ogni port delle fasi precedenti (LLM/Email/Payment/Notification/connector); le sorgenti **interne** (affiliazioni/newsletter/social/contenuti) sono invece **dati reali** letti dal DB | Nessun dato di traffico reale: GA4 (sessioni/utenti per canale) e Search Console (impressioni/click/posizione) sono fixture; OAuth2 + property/site id + key management non esistono. Inoltre oggi gli stub girano **dentro** la write-tx di ingest (innocuo perché sincroni e deterministici); un adapter reale deve fare il fetch **fuori** dalla tx | **Prima di collegare GA4/Search Console reali** → `Ga4Source`/`SearchConsoleSource` su API reale (OAuth2 + key management, coerente con DEBT-008) con fetch **fuori** dalla write-tx; segue follow-up del fondatore | **OPEN** |
| DEBT-014 | **Il loop di feedback non è ancora auto-iniettato nella generazione live**: `FeedbackService` deriva il segnale e lo espone (`GET /feedback/proposal`), e il `promptHint` è cablato in `buildPrompt`/`generateDraft` (`platform/ai/pipeline.ts`, provato), ma l'endpoint di generazione articolo (`verticals/travel/itineraries.controller.ts`) **non** legge il `feedbackHint` da `FeedbackService` (passa solo `voice`) | Slice 4.2 = il **loop** (le metriche cambiano la proposta) — l'accettazione è il piano/segnale che cambia; il bridge nel prompt è dimostrato e testato, ma l'auto-iniezione al call-site live è legata al **motore di autonomia** ("auto-within-limits", oggi stub in Settings), fuori scope dello slice | Una proposta adattata dai dati esiste e si vede in `/analytics`, ma la **generazione automatica** del prossimo articolo non onora ancora il segnale finché un umano non lo applica; per il tenant #2 / l'autonomia reale andrà cablato | **Quando si costruisce il motore di autonomia** (o al tenant #2) → l'endpoint di generazione legge il `promptHint` da `FeedbackService` e lo passa a `generateDraft` (coerente con DEBT-010, stessa via di generazione) | **OPEN** |
| DEBT-012 | **Instradamento cliente WhatsApp/mail = solo stub deterministico** (`NotificationPort`/`StubNotificationClient` in `modules/crm`): il router reale (WhatsApp Business API + gamba mail su `EmailPort`) di [ADR-0024](adr/0024-crm-custom-trip-pipeline-notification-port.md) non è implementato | Il journey CRM (*richiesta→proposta→approva→acconto→conferma→consegna*) è verso uno stub al confine — niente WhatsApp/SMTP reale/rete/non-determinismo in CI, come gli altri port (LLM/Email/Payment/connector); lo stub **registra** i messaggi così il cancello human-in-the-loop è asseribile | Nessuna notifica reale: nessun messaggio WhatsApp/mail parte verso il cliente; lo `whatsapp` usa `customerEmail` come handle (manca un campo telefono per-lead); template/consegna/stato di recapito non esistono | **Prima del primo invio reale a un cliente** → `WhatsAppNotificationClient` (Business API) + delega della gamba mail all'`EmailPort` (DEBT-007) + campo telefono per-lead + key management (coerente con DEBT-008); segue follow-up del fondatore | **OPEN** |
| DEBT-015 | **Risoluzione del tenant a runtime non implementata**: `TenancyService.current()` ritorna sempre `FOUNDER_TENANT_ID`; inoltre `POST /tenants` è protetto dal **JWT del fondatore**, non da un ruolo admin dedicato | Phase 4.3 ha pagato l'**isolamento dei dati** (onboarding reale + RLS+grant su tutta la superficie, provato come `app_rw`); la *risoluzione* del tenant per-richiesta e l'auth multi-utente sono uno slice a sé (l'auth è founder-only, ADR-0010) | Un secondo tenant è isolato a livello dati ma **non può ancora fare login** e vedersi le richieste auto-scopate; l'endpoint di onboarding non distingue founder da super-admin | **Quando il tenant #2 deve operare davvero (login/multi-utente)** → risolvere `tenant_id` dalla request (JWT/host) in `TenancyService` + ruolo admin dedicato per l'onboarding | **OPEN** |
| DEBT-016 | **Prezzi dei modelli Anthropic hard-coded** in `platform/ai/model-registry.ts` (`PRICING_PER_MTOK`: in/out/cache-read per tier `fast`/`balanced`/`powerful`) | Una tabella prezzi statica è l'unica fonte da aggiornare; `pricePerToken`/`estimateWorstCaseUsd` restano funzioni pure testabili senza rete né dipendenze | Un cambio di listino Anthropic non si riflette finché qualcuno non aggiorna il file → stima costo / circuit-breaker (R1-B) calcolano su prezzi vecchi | **Al primo cambio di prezzi Anthropic** (o quando il metering reale di R1-B va in produzione) → aggiornare `PRICING_PER_MTOK` (e valutare una fonte esterna/config) | **OPEN** |
| DEBT-017 | **`AnthropicLlmAdapter` passa un `input_schema` permissivo** (`{ type: "object" }`) per ogni tool invece del JSON Schema reale del tool | In R1-A non esistono ancora tool reali né chiamate reali all'adapter (CI è tutta su `StubLlmAdapter`, zero-cost); il `ToolDefinition` usa `SchemaLike` (Zod-compatibile) ma non espone un JSON Schema | Con tool reali, un adapter che non descrive i parametri al modello degrada la qualità del tool-use (il modello "indovina" gli argomenti) | **Alla prima slice con tool reali (A1-writer)** → derivare il JSON Schema dal `ToolDefinition` (es. `zod-to-json-schema`) e passarlo all'SDK | **OPEN** |
| DEBT-019 | **Metering + circuit-breaker (R1-B) costruiti ma non ancora cablati nel path LLM di runtime**: `MeteringService`/`BudgetGuard`/`MeteredLlmAdapter` sono provati (unit + integration come `app_rw`), ma (a) i caller di produzione non passano per `MeteredLlmAdapter` (`generateDraft` riceve un `LlmPort` iniettato; `infra.module` usa ancora il `LlmClient` legacy) → nessun costo registrato né tetto applicato a runtime; (b) `BudgetGuard.resolveBudgetUsd` è **iniettato**, non ancora collegato a `tenant_settings.budgetUsdMonthly` in un call-site live; (c) `ai_usage_events.run_id` è nullable **senza vincolo FK** perché `ai_agent_runs` non esiste ancora | R1-B ha scope `platform/ai/` + schema/migration: il metering è il **meccanismo**, non il cablaggio runtime; comporre il port metered richiede l'`AgentRunner` (A1-core) e `ProviderRegistry` (R1-C), fuori scope. Funzioni pure + decorator provati in isolamento (zero-cost, `StubLlmAdapter`) | Finché non cablato, il budget per-tenant non è enforce in produzione e il join run→costo non è vincolato lato DB (un `run_id` orfano è possibile) | **Ad A1-core / A1-writer** → l'`AgentRunner` compone `metered(provider(tenant))`, risolve `budgetUsdMonthly` da `getTenantSettings`, e la migration di `ai_agent_runs` aggiunge il **FK** `ai_usage_events.run_id → ai_agent_runs.id` | **OPEN** |
| DEBT-018 | **Coesistenza seam legacy `LlmClient` ↔ nuovo `LlmPort`**: `platform/ai/llm.ts` mantiene `LlmClient`/`AnthropicLlmClient`/`StubLlmClient`/`createLlmFromEnv(): LlmClient` accanto a `LlmPort`; i caller non migrati (generatore articolo `verticals/travel/article.ts`, proposta CRM `modules/crm/proposal.ts`, i rispettivi controller, `infra.module` token `LLM`) usano ancora `LlmClient` | R1-A ha scope `platform/ai/` + i test che istanziano l'LLM: toccare moduli/verticali era fuori scope e avrebbe rotto journey verdi (DO NO HARM). Solo `generateDraft` (senza caller di produzione) è migrato a `LlmPort` | Due seam LLM in parallelo finché i caller restano sul vecchio contratto; `createLlmFromEnv` legacy resta esportato da `llm.ts` (ma **non** dal barrel pubblico, che espone solo la versione `LlmPort` via alias) | **Quando i caller diventano agenti** (A1-writer per il Writer; slice CRM/inbound per la proposta) → migrarli a `AgentRunner`/`LlmPort` via `ProviderRegistry` (R1-C) e ritirare `LlmClient` + il `createLlmFromEnv` legacy | **OPEN** |
| DEBT-020 | **`AgentDefinition` statiche nel codice, nessuna tabella registry**: `AgentRegistry` (`platform/ai/agent-registry.ts`) tiene le definizioni in memoria; `version(id)` è un hash JSON-stabile dei campi identificanti, ma non c'è persistenza/versionamento DB delle definizioni né storia delle versioni | A1-core costruisce il **runtime** generico con un solo agente-stub di prova; le definizioni reali (Writer/SEO/Social/…) e la loro evoluzione arrivano nelle slice successive — una tabella ora sarebbe vuota e speculativa | Una modifica a una definizione cambia il `agentDefinitionVersion` (snapshottato su ogni run/proposal) ma la versione precedente non è interrogabile a DB; niente rollout/rollback per-tenant di una definizione né audit "quale versione girava il giorno X" oltre allo snapshot | **Quando le definizioni diventano configurabili per-tenant** (o serve storia/rollout delle versioni) → tabella `ai_agent_definitions` (RLS se per-tenant) + lookup nel registry | **OPEN** |
| DEBT-024 | **`getFeedbackSignal` non cablato al call-site + pre-iniezione del segnale non ancora prodotta**: (a) il tool `getFeedbackSignal` esiste con `stubArgs()` e applica `deriveFeedbackSignal`/`buildContentProposal` su un `AnalyticsDashboard`, ma **nessun caller reale inietta l'accessor** (cugino di DEBT-022.c) — il path `generateDraft` non passa `getFeedbackSignal` né `contentItemId`, quindi il tool non è offerto sulla superficie live; l'accessor (fetch da `metric_snapshots` via `FeedbackService`/analytics sotto RLS) va fornito dal controller; (b) la via **preferita** (pre-iniezione del `feedbackHint` nel brief) richiede l'Orchestratore/piano editoriale che la produca — finché non esiste, in stand-alone il modello deve chiamare il tool, e il `contentItemId` non è ancora propagato dal call-site (cugino di DEBT-014) | A2 valida il **feedback loop** del Writer (tool deterministico + scelta pre-iniezione vs tool-call) mantenendo `generateDraft` **identico** (DO NO HARM); il cablaggio dell'accessor dal controller e la produzione del segnale pre-iniettato dall'Orchestratore sono slice a sé (integrazione controller / Orchestratore), fuori scope | Finché non cablato: l'auto-miglioramento da metriche è provato a livello di `WriterAgent`/tool ma non raggiungibile da un run reale; nessun adattamento metrico sul path travel live finché il `contentItemId`/accessor non sono propagati | **Quando il controller diventa caller dell'agente** → iniettare l'accessor `getFeedbackSignal` (fetch `metric_snapshots` sotto RLS) e propagare `contentItemId`; **e/oppure alla slice Orchestratore** → produrre il `feedbackHint` pre-iniettato nel brief (via preferita, zero tool-call) | **OPEN** |
| DEBT-023 | **BYOK: chiave LLM nell'envelope `OAuthToken` riusato + nessun flusso di provisioning + `ProviderRegistry` non ancora cablato al path live**: (a) la chiave Anthropic per-tenant riusa `connector_credentials` (AES-256-GCM, RLS) ma viaggia nello slot `accessToken` dell'`OAuthToken` (campi `refreshToken`/`expiresAt` inutilizzati per una chiave LLM) — manca un envelope di prima classe per "API key" e non c'è onboarding/UI per inserire/ruotare la chiave (cugino di DEBT-008); (b) `WriterAgent` accetta il `ProviderRegistry` ma il wrapper `generateDraft` (path travel live) passa ancora la **chiave-piattaforma** via il token DI `LLM` (`infra.module.ts` → `createLlmFromEnv`), quindi BYOK **non è attivo** sulla superficie travel finché il `ProviderRegistry` non è cablato nella DI con il `DbCredentialStore`; (c) il campo `TenantSettings.aiProvider` è **solo metadata** e non è ancora scritto/sincronizzato quando una credenziale `llm_anthropic` viene creata | R1-C ha scope `platform/ai/` + contratto settings: il `ProviderRegistry` è la sorgente unica del `LlmPort` e il Writer la consuma; il cablaggio DI end-to-end (provider+credential store nel token `LLM`, con `metered(provider(tenant))`) e l'onboarding della chiave sono slice a sé (T1 / integrazione controller / DEBT-008), fuori scope; comportamento di `generateDraft` mantenuto **identico** (DO NO HARM) | Finché non cablato: BYOK è provato a livello di `ProviderRegistry`/`WriterAgent` ma il path travel live continua a usare la chiave-piattaforma; la chiave LLM condivide la forma dei token OAuth (nessuna scadenza/refresh reali); `aiProvider` può divergere dallo stato reale (la credenziale, non il campo, decide) | **A slice T1 (coda `agent_proposals` con `metered(provider(tenant))`)** → cablare `ProviderRegistry` (+ `DbCredentialStore`) nella DI come sorgente del `LlmPort` per gli agenti; **e/oppure** quando si aggiunge l'onboarding della chiave (con DEBT-008) → envelope dedicato per le API key + scrittura di `aiProvider='anthropic'` alla creazione della credenziale | **OPEN** |
| DEBT-022 | **Sink provvisorio della bozza del Writer + tools di modulo/vertical non ancora cablati al call-site**: (a) `WriterAgent.run` emette un `Proposal<ArticleDraft>`, ma finché non arriva la coda `agent_proposals` (slice T1) il `generateDraft` wrapper **scarta l'envelope del Proposal** e ritorna il `DraftResult` di sempre → la bozza atterra nel **sink della macchina a stati Fase 1** (nessuno staging `agent_proposals`, nessun gate "proposta" dedicato); (b) sul path `generateDraft` il runner gira con **store/budget no-op** (nessuna riga `ai_agent_runs`, nessun metering) per non cambiare il comportamento esterno; (c) i tool `getBrandVoice`/`getItinerary`/`getMediaForStop` esistono con `stubArgs()` ma **non sono cablati** da un caller reale (solo `retrieveContext` è iniettato da `generateDraft`) — gli accessor verso travel/media vanno forniti dal controller (`itineraries.controller.ts`) | A1-writer valida il **pattern** (Writer su `AgentRunner` + tool iniettati + gate autenticità) mantenendo `generateDraft` **identico** per i caller (DO NO HARM); la coda proposte reale, il metering/audit sul path live e il cablaggio degli accessor di modulo sono slice a sé (T1 / integrazione controller), fuori scope | Finché non cablato: la bozza del Writer non transita per uno staging proposte interrogabile (vive nel sink Fase 1 come prima); nessun costo/audit registrato sul path `generateDraft`; gli altri tre tool sono provati ma non raggiungibili da un run reale | **A slice T1 (coda `agent_proposals`)** → il wrapper consuma il `Proposal` nella coda di staging con store/budget reali (`metered(provider(tenant))` + `budgetUsdMonthly`); **e/oppure** quando il controller travel diventa caller dell'agente → iniettare gli accessor `getItinerary`/`getMediaForStop`/`getBrandVoice` | **OPEN** |
| DEBT-021 | **`ai_agent_runs`: nessun TTL/archivio, payload del Proposal in `usage_json` interinale, FK `ai_usage_events.run_id` ancora differita**: (a) la tabella cresce senza retention; (b) il `Proposal` (payload/rationale/costo/usage/truncated) è snapshottato nell'envelope `usage_json` come **staging interinale** finché `agent_proposals` (slice T1) non è la coda reale; (c) il FK `ai_usage_events.run_id → ai_agent_runs.id` previsto da DEBT-019 **non** è aggiunto — il metering scrive `ai_usage_events` **dentro** il loop (per chiamata), prima che la riga `ai_agent_runs` esista (scritta best-effort a fine run), quindi un FK la violerebbe; (d) l'idempotenza è best-effort, legata alla scrittura audit: se l'audit fallisce non c'è riga → un retry **ri-esegue** | A1-core ha scope `platform/ai/` + schema/migration: la coda proposte reale, la retention e il pre-inserimento di una riga `pending` (necessario per il FK) sono slice a sé (T1 / hardening); l'envelope tiene insieme audit + replay idempotente senza trascinare `agent_proposals` ora | Crescita non limitata di `ai_agent_runs`; il join run→costo resta non vincolato a DB (un `run_id` orfano è possibile, come già in DEBT-019); il payload del Proposal vive in una colonna audit invece che nella coda proposte; un crash dopo il consumo LLM ma prima dell'audit fa ri-pagare il run al retry | **A slice T1 (coda `agent_proposals`)** → spostare il payload nella coda; pre-inserire la riga run come `pending` a inizio loop e aggiungere il **FK** `ai_usage_events.run_id → ai_agent_runs.id`; aggiungere retention/archivio per `ai_agent_runs` | **OPEN** |

> **DEBT-005 → PAID — rinforzato (2026-06-01):** Phase 4.3 aggiunge un **audit eseguibile** su tutta la superficie (`onboarding.integration.test.ts`, come `app_rw`): per ognuna delle 17 tabelle tenant-scoped verifica RLS `ENABLE`+`FORCE` + policy **e** i grant `app_rw` (SELECT/INSERT/UPDATE/DELETE), che `tenants` è di sola lettura per `app_rw`, e l'**accettazione** cross-modulo (onboarding del tenant #2 → scrittura rappresentativa in ogni tabella per due tenant → nessuna lettura/leak incrociato). Il debito resta PAID; ora è anche **guardato da un test** che fallisce se una tabella futura nasce senza RLS+grant (il fallimento dello slice 4). Apre **DEBT-015** (risoluzione tenant a runtime), fuori dall'isolamento dei dati.

> **DEBT-001 → PAID (2026-05-29):** repo spostato da OneDrive a `C:\progetti-ai\blogs-manager` (NTFS, non sincronizzato) → risolti sync/lock. Caveat: è un path **Windows nativo, non WSL2**, quindi l'I/O Docker/file-watch può restare meno fluido che in WSL2 → scelta registrata in **ADR-0011** (Windows nativo, WSL2 rimandato).

> **DEBT-002 → PAID (2026-05-29):** `schema.ts` è ora la **fonte tipata unica** (incl. `content_embeddings` con colonna `vector(256)`); `rag.ts` usa query Drizzle tipizzate (niente cast raw); **drizzle-kit cablato** (`drizzle.config.ts`, script `db:generate/migrate/check`, snapshot in `drizzle/meta/`). Il baseline `0000_init.sql` è generato da drizzle-kit e rifinito a mano per estensione pgvector + RLS (vedi `apps/api/drizzle/README.md`).

> **DEBT-004 → PAID (2026-05-29):** aggiunto **swc** a Vitest (`vitest.http.config.ts` + `unplugin-swc`) per i decorator metadata di Nest; nuovo test automatico `auth.http.test.ts` che boota l'app Nest e verifica `/auth/login` + `/auth/me` (200/401) via supertest. Gira in CI (step `test:http`).

> **DEBT-003 → PAID (2026-05-29):** branch protection attiva su `main` (ruleset): richiesto il check **CI**, *require branches up to date*, *block force pushes*. Un commit che non passa la CI non può finire su `main` (va prima validato su un altro ref → PR). **Registro debito azzerato.**

> **DEBT-009 → PAID (2026-05-31):** aggiunto il read-model di lista in `modules/content`: `listContentItems(tx, {type?, status?})` (`content.repo.ts`, esposto dal barrel) e l'endpoint `GET /articles` (con `?type=` / `?status=`) in `articles.controller.ts`, dietro tenant guard + RLS (`withTenant`). Provato da `articles.list.http.test.ts` (lista per-tenant, filtri type/status combinabili, **isolamento RLS** cross-tenant). La Library (slice 1) consuma questo endpoint. **Registro debito di nuovo azzerato** (aperti restano solo DEBT-006/007/008, non scaduti).

> **DEBT-005 → PAID (2026-05-30):** pagato **in anticipo** rispetto al trigger (su richiesta, per ripartire puliti). Il bootstrap, su connessione **admin** (`DATABASE_ADMIN_URL`), provisiona un ruolo app **`app_rw` `NOSUPERUSER`** con `GRANT` mirati (`ensureAppRole` in `platform/db/bootstrap.ts`); l'app gira come `app_rw` (`DATABASE_URL`) → **RLS enforced a runtime**. Provato da `runtime-rls.integration.test.ts` (isolamento cross-tenant + grant sull'intera catena Fase 1) e dal boot reale dell'app (journey verde, 0 warning "bypass"). Realizza l'hardening previsto da **ADR-0002**. **Registro debito di nuovo azzerato.**

---

## Come aggiungere una voce
1. Nuova riga con `DEBT-00N`, descrizione, *perché*, rischio, **trigger** concreto (mai "dopo").
2. `// TODO(debt): DEBT-00N` nel punto del codice interessato.
3. Quando il trigger scatta → `PAYING` → risolto → `PAID` (mantieni la riga per storia).
